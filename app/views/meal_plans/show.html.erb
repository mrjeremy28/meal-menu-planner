<p style="color: green"><%= notice %></p>

<div class="d-flex justify-content-end">
    
  <%= link_to("Edit", edit_meal_plan_path(@meal_plan), class: 'action edit btn btn-primary') %>
  <%= link_to("Delete", meal_plan_path(@meal_plan), method: :delete, class: 'action delete btn btn-danger') %>
</div>

<%= render @meal_plan %>

